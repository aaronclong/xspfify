[project]
name = "xspfify"
version = "0.1.0"
description = "CLI tool to export spotify playlists to flat files"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "spotipy>=2.25.1",
    "xspf-lib>=0.3.4",
]

[dependency-groups]
dev = [
    "ruff>=0.14.5",
]
